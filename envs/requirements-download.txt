# Dedicated YouTube download environment (.venvs/download).
# Keep this requirement unbounded: setup_download_env.sh uses --upgrade so
# provisioning refreshes yt-dlp and its bundled EJS challenge solver.
yt-dlp[default]
soundfile>=0.14.0
pysocks>=1.7.1
httpx[socks]>=0.28.1
