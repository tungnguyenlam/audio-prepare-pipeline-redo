# VibeVoice-ASR transcription and purity verification needs Transformers 5.3+ for
# VibeVoiceAsrForConditionalGeneration. Keep it in .venv-vibevoice rather
# than changing the primary uv environment. bitsandbytes is required for
# the selective INT8 / NF4 Hugging Face checkpoints.
# openai-whisper and whisper-timestamped are required for word-level alignment
# in scripts/asr/vibevoice.py (default --align-model vinai/PhoWhisper-small).
transformers>=5.3.0
accelerate>=1.0.0
bitsandbytes>=0.48.1
pysocks>=1.7.1
httpx[socks]>=0.28.1
openai-whisper
whisper-timestamped>=1.15.0
librosa>=0.11.0
